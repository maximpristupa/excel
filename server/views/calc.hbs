<!DOCTYPE html>
<html>
<head>
    <title>Csv</title>
    <meta charset="utf-8" />
</head>
<body>
	<form action="/uploadfile" enctype="multipart/form-data" method="POST"> 
    	<input type="file" name="myFile" />
    	<input type="submit" value="Upload a file"/>
    </form>

    <form action="/calculate" method="POST"> 
        {{#if csv}}
            {{#each csv}}
            	<div class="row">
            		{{#each this}}
            			<div class="cell" style="display:inline-block; margin: 10px; text-align: center;">
            				<input type="text" name="cell" value="{{this}}" style="width: 100px; height: 100px; font-size: 20px;">
            		    </div>
            		{{/each}}		
            	</div>
            {{/each}}
            <input type="hidden" name="rowCount" value="{{rowCount}}">
            <input type="submit" value="Recalculate"/>
        {{/if}}
    </form>
</body>
<html>